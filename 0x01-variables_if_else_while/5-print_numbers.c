#include <stdio.h>

/**
 *main-Entry point
 *
 *Description: print value of n satus; positive, negative or zero
 *
 * Return: Always 0 (success)
*/

int main(void)
{
int f;
for (f = 0 ; f < 10 ; f++)
{
	printf("%d", f);
}
printf("\n");
return (0);
}
